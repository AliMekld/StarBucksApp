class Coffe {
  // model of each coffee in the shop
  final String name;
  final String price;
  final String imagePath;
  // gernerate constructor to path data throuh the app
  Coffe(this.name, this.price, this.imagePath);
}
